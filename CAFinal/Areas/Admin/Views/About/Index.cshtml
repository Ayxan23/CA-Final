@model List<About>
@{
    ViewData["Title"] = "About";
}
<div class="content-wrapper">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">About</h4>
                    <div class="table-responsive">
                        <a class="btn btn-primary @(ViewBag.Count == 1 ? "disabled" : "")" asp-action="Create">Create</a>
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Image</th>
                                    <th>Title</th>

                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var about in Model)
                                {
                                    <tr>
                                        <td>
                                            <img src="~/assets/img/@about.Image" />
                                        </td>
                                        <td>@about.Title</td>
                                        <td class="button_box">
                                            <a class="btn btn-success" asp-action="Detail" asp-route-id="@about.Id">Detail</a>
                                            <a class="btn btn-warning" asp-action="Update" asp-route-id=@about.Id>Update</a>
                                            <a class="btn btn-danger" asp-action="Delete" asp-route-id=@about.Id>Delete</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

